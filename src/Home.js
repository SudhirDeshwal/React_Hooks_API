import React from 'react';

const Home = (props)=> {

    return (
         <div> 
        
          <h4>This is Home: This app is combination of all react technologies we have used till now.</h4>
         </div>
         
         );
      };

    export default Home;